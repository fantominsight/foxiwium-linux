---
# Extra services to enable on the installed system (in addition to what the
# shipped rootfs already enables via the overlay: NetworkManager, ssh, sddm).
units:
    - name: NetworkManager
      action: enable
    - name: ssh
      action: enable
    - name: sddm
      action: enable
